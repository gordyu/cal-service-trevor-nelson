apps:
  - script   : server/server.js
    instances: 4
    exec_mode: cluster
  - script :  server/server.js
    watch  : true
    env    :
      NODE_ENV: development
    env_production:
      NODE_ENV: production