apps:
  - script   : server/server.js
    instances: 1
    exec_mode: cluster
  - script :  server/server.js
    watch  : true
    env    :
      NODE_ENV: production
    env_production:
      NODE_ENV: production